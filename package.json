{
  "name": "content-area-item-options",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "pack": "dotnet pack --configuration Release && bun run index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}